<div style="margin: 70px 0px 0px 70px" >
  <h4 id="title">ご褒美を登録する</h4>
  <br>
  <%= render partial: 'rewards/form', locals: { reward: @reward } %>

  <br><br><br>

  <thead>
    <tr>
        <h4 id="title">一覧</h4>
        <th colspan="3"></th>
    </tr>
  </thead>

  <div id="rewards_area">
    <%= render partial: 'rewards/lists', locals: { rewards: @rewards } %>
  </div>
</div>
